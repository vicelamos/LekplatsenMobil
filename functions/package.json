{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "private": true,
  "type": "module",
  "main": "index.mjs",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions,firestore",
    "serve:dev": "firebase use development && firebase emulators:start --only functions,firestore",
    "serve:prod": "firebase use production && firebase emulators:start --only functions,firestore",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "deploy:dev": "firebase use development && firebase deploy --only functions",
    "deploy:prod": "firebase use production && firebase deploy --only functions",
    "logs": "firebase functions:log",
    "logs:dev": "firebase use development && firebase functions:log",
    "logs:prod": "firebase use production && firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.1.1"
  },
  "devDependencies": {
    "eslint": "^8.15.0",
    "eslint-config-google": "^0.14.0"
  }
}
